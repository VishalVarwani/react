import React from 'react';
import ReactDOM from 'react-dom';
import PmageEditor from './Imageeditor';

ReactDOM.render(
  <React.StrictMode>
    
    <PmageEditor/>
  
  </React.StrictMode>,
  document.getElementById('root')
);
